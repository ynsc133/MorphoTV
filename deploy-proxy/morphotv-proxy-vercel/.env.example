# MorphoTV 代理服务器环境变量配置
# 复制此文件为 .env.local 并根据需要修改配置

# 允许的域名白名单（可选）
# 如果设置，只有这些域名的请求会被代理
# 多个域名用逗号分隔
# ALLOWED_DOMAINS=api.example.com,another-api.com

# Node.js 环境
NODE_ENV=production

# 禁用 Next.js 遥测
NEXT_TELEMETRY_DISABLED=1

# 自定义配置（可选）
# CUSTOM_KEY=your-custom-value

# Vercel 部署配置（自动设置，无需手动配置）
# VERCEL=1
# VERCEL_URL=your-app.vercel.app
# VERCEL_REGION=iad1
